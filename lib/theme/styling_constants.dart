import 'package:flutter/cupertino.dart';

class MyPadding {
  static const EdgeInsets tilePadding = EdgeInsets.symmetric(horizontal: 16, vertical: 10);
  static const EdgeInsets pagePadding = EdgeInsets.all(24.0);
}